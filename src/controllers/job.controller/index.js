const getJobs = require('./getJob');
const applyForJob = require('./applyForJob');
const eligibleForJob = require('./eligibleForJob');

module.exports = {
  getJobs,
  applyForJob,
  eligibleForJob,
};
